import { Operator } from '../types/Operator'
import { extendCase } from '../utils/operator'

export const assignSynonyms: Operator[] = extendCase([
  // disables custom calculations
  // Use below code to activate custom calculations
  /*
    { label: ':=', field: ':=' }
  */
])

